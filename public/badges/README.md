Upload badge artwork here before launch.

Expected filenames:

- `streak-2.png`, `streak-5.png`, ... `streak-140.png`
- Optional locked variants: `streak-2-locked.png`, `streak-5-locked.png`, ... `streak-140-locked.png`

The achievements page keeps its emoji fallback if an image is missing.
